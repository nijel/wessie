$Id$

ORDERED by importance (order of starting work on it):

add category editing

add downloads editing + write download script

images for languages

optimalise left menu - select all menu items for current category and then write it from array
- use only ane select for menu+page tables

add pages listing + links to edit (this probably needs plugins working)

top menu of administration - list also installed plugins, show multiple row

plugin functionality!

create plugin for news

create plugin for simple items listing - name + value - listed on pages by n records

cicom/cihar? test implementation

add users + ACL + administration of it

add options editing, sections:
-languages + default pages
-db + table names
-site config
-admin config

add consistency checks (when page removed, there should retain no references to it - at least in system part - menus, default pages etc.)


insert new row for week stat at the start of week - some cache!!!!!
cache for categories?


WHAT? valid from?


plugins:
	5 discussions
	5 mailing lists

scripts:
	2 administration
	5 download script
	1 banners - monitor views and CTRs
	          - allow campaigns with n views / clicks?
	          - allow per page/category/etc. ads
	          - administration by client:
	             - change img/url
                 - view stats
                 - start/stop

other:
	1 custom error page

configuration of:
	3 HTTP headers
	4 counter/stats working

update FUNCTIONS,VARIABLESm write README and some help
